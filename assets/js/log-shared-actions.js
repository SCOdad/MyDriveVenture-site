(() => {
  const app=window.DV_LOG_APP;if(!app?.client)return;const client=app.client;
  if(!document.querySelector('link[data-dv-shared-log-fixes]')){const l=document.createElement('link');l.rel='stylesheet';l.href='/assets/css/log-shared-fixes.css?v=20260818-1620';l.dataset.dvSharedLogFixes='true';document.head.appendChild(l)}
  const esc=v=>String(v??'').replace(/[&<>'\"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt',"'":'&#39;','\"':'&quot;'}[c]));
  const setStatus=(text,kind='')=>{const el=document.getElementById('vehicle-status');if(el){el.textContent=text||'';el.className=`app-status${kind?` ${kind}`:''}`}};
  const oldForm=document.getElementById('vehicle-form');if(oldForm){const form=oldForm.cloneNode(true);oldForm.replaceWith(form);form.addEventListener('submit',async e=>{e.preventDefault();if(!form.reportValidity())return;const driverId=app.getDriverId();setStatus('Adding vehicle…');const{data,error}=await client.rpc('add_authenticated_vehicle_v1',{p_driver_id:driverId,p_name:document.getElementById('vehicle-name').value.trim(),p_vehicle_class:document.getElementById('vehicle-class').value,p_color:document.getElementById('vehicle-color').value.trim()||null,p_is_primary:app.getActiveVehicles().length===0});if(error||!data?.ok)return setStatus(`Garage: ${error?.message||data?.error||'Unable to add vehicle.'}`,'error');form.reset();await app.refreshDashboard();setStatus('Vehicle added.','success')})}
  const oldList=document.getElementById('vehicle-list');if(oldList){const list=oldList.cloneNode(true);oldList.replaceWith(list);list.addEventListener('click',async e=>{const b=e.target.closest('[data-archive-vehicle]');if(!b)return;setStatus('Updating Garage…');const{data,error}=await client.rpc('archive_authenticated_vehicle_v1',{p_driver_id:app.getDriverId(),p_vehicle_id:b.dataset.archiveVehicle});if(error||!data?.ok)return setStatus(`Garage: ${error?.message||data?.error||'Unable to archive vehicle.'}`,'error');await app.refreshDashboard();setStatus('Vehicle archived.','success')})}
  const achievements=document.getElementById('quest-list')?.closest('.app-card');if(achievements)achievements.id='achievements';
  function annotate(model,id){const help=new Map((model.quest_awards||[]).filter(q=>q.driver_id===id).map(q=>[q.quest?.name||q.quest_key,q.quest?.description||'']));document.querySelectorAll('#quest-list .quest-item').forEach(item=>{const t=item.querySelector('strong'),d=help.get((t?.textContent||'').trim());if(d){item.dataset.questHelp=d;t.classList.add('quest-help-target');t.tabIndex=0;t.title=d}})}
  async function renderArchived(driverId){const list=document.getElementById('vehicle-list');if(!list)return;list.querySelector('.archived-vehicles-wrap')?.remove();const{data,error}=await client.rpc('get_authenticated_archived_vehicles_v1',{p_driver_id:driverId});if(error||!data?.ok)return;const rows=data.vehicles||[];if(!rows.length)return;list.insertAdjacentHTML('beforeend',`<li class="archived-vehicles-wrap"><details class="archived-vehicles"><summary>Show Archived (${rows.length})</summary><ul>${rows.map(v=>`<li class="vehicle-item archived-vehicle"><div><strong>${esc(v.name)}</strong><br><small>${esc(v.vehicle_class)}${v.color?` · ${esc(v.color)}`:''} · Archived</small></div></li>`).join('')}</ul></details></li>`)}
  let token=0;async function mission(driverId){const radio=document.querySelector('.radio-display');if(!radio)return;const mine=++token;radio.innerHTML='<span>ON AIR / MISSION CONTROL</span><p class="mission-loading">Tuning recommended quests…</p>';const{data,error}=await client.rpc('get_authenticated_quest_recommendations_v1',{p_driver_id:driverId});if(mine!==token)return;if(error||!data?.ok){radio.innerHTML='<span>ON AIR / MISSION CONTROL</span><p class="mission-loading">Recommendations unavailable right now.</p><a class="mission-all" href="#achievements">VIEW ALL ACHIEVEMENTS ↓</a>';return}const qs=data.quests||[];if(!qs.length){radio.innerHTML='<span>ON AIR / MISSION CONTROL</span><p class="mission-loading">No recommended quests right now. Keep driving!</p><a class="mission-all" href="#achievements">VIEW ALL ACHIEVEMENTS ↓</a>';return}const f=qs[0],x=qs.slice(1,8);radio.innerHTML=`<span>ON AIR / MISSION CONTROL</span><div class="mission-featured" data-quest-help="${esc(f.description)}"><small>FEATURED QUEST</small><strong class="quest-help-target" tabindex="0" title="${esc(f.description)}">${esc(f.name)}</strong><em>${esc(f.progress_text||f.description)}</em><div class="mission-progress"><i style="width:${Math.max(3,Number(f.progress_percent||0))}%"></i></div></div>${x.length?`<ol id="radio-quest-list" class="mission-list">${x.map(q=>`<li data-quest-help="${esc(q.description)}"><strong class="quest-help-target" tabindex="0" title="${esc(q.description)}">${esc(q.name)}</strong><small>${esc(q.progress_text||q.description)}</small></li>`).join('')}</ol>`:''}<a class="mission-all" href="#achievements">VIEW ALL ACHIEVEMENTS ↓</a>`}
  window.addEventListener('dv:dashboard-rendered',e=>{const{model,driverId}=e.detail||{};if(!model||!driverId)return;annotate(model,driverId);renderArchived(driverId);mission(driverId)});
  const touch=()=>matchMedia('(hover: none)').matches||matchMedia('(pointer: coarse)').matches||innerWidth<=760;document.addEventListener('click',e=>{const t=e.target.closest('.quest-help-target');if(!t||!touch())return;const item=t.closest('[data-quest-help]');if(!item)return;document.querySelectorAll('.quest-help-open').forEach(x=>{if(x!==item)x.classList.remove('quest-help-open')});item.classList.toggle('quest-help-open')});
  const model=app.getModel(),id=app.getDriverId();if(id){annotate(model,id);renderArchived(id);mission(id)}
})();