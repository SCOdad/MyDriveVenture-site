(()=>{let linksByFeedback=new Map();const originalFetch=window.fetch.bind(window);window.fetch=async(...args)=>{const response=await originalFetch(...args);try{const request=args[1],body=request?.body?JSON.parse(request.body):null;if(body?.action==='list'){const clone=response.clone(),out=await clone.json();if(out?.ok){linksByFeedback=new Map();for(const link of out.links||[]){if(!linksByFeedback.has(link.feedback_code))linksByFeedback.set(link.feedback_code,[]);linksByFeedback.get(link.feedback_code).push(link)}}}}catch{}return response};function esc(s){return String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}function relationLine(label,items){if(!items?.length)return'';return `<div class="feedback-relations"><strong>${label}</strong>${items.map(x=>`<a href="/operator/backlog/?bklg=${encodeURIComponent(x.backlog_code)}"><span>${esc(x.backlog_code)}</span> — ${esc(x.title||'Untitled')} — ${esc(x.status||'status unavailable')}</a>`).join('')}</div>`}function decorate(){const code=document.getElementById('selected-code')?.textContent?.trim();if(!code)return;const links=linksByFeedback.get(code)||[],container=document.getElementById('linked-backlog-list');if(!container||!links.length)return;const anchors=[...container.querySelectorAll('.backlog-link')];for(const a of anchors){const backlogCode=a.querySelector('strong')?.textContent?.trim(),link=links.find(x=>x.backlog_code===backlogCode);if(!link||a.querySelector('.feedback-relations'))continue;a.insertAdjacentHTML('beforeend',relationLine('Depends on',link.dependencies)+relationLine('Depended on by',link.dependents))}}const observer=new MutationObserver(decorate);window.addEventListener('DOMContentLoaded',()=>{const target=document.getElementById('linked-backlog-list');if(target)observer.observe(target,{childList:true,subtree:true});decorate()})})();