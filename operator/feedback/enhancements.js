(()=>{let linksByFeedback=new Map(),authHeader='';const cache=new Map(),originalFetch=window.fetch.bind(window);window.fetch=async(...args)=>{const response=await originalFetch(...args);try{const request=args[1]||{},body=request.body?JSON.parse(request.body):null;if(request.headers){const h=request.headers instanceof Headers?request.headers.get('authorization'):request.headers.authorization||request.headers.Authorization;if(h)authHeader=h}if(body?.action==='list'){const out=await response.clone().json();if(out?.ok){linksByFeedback=new Map();for(const link of out.links||[]){if(!linksByFeedback.has(link.feedback_code))linksByFeedback.set(link.feedback_code,[]);linksByFeedback.get(link.feedback_code).push(link)}}}}catch{}return response};function esc(s){return String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}function relationLine(label,items){if(!items?.length)return'';return `<div class="feedback-relations"><strong>${label}</strong>${items.map(x=>`<a href="/operator/backlog/?bklg=${encodeURIComponent(x.backlog_code)}"><span>${esc(x.backlog_code)}</span> — ${esc(x.title||'Untitled')} — ${esc(x.status||'status unavailable')}</a>`).join('')}</div>`}async function detail(code){if(cache.has(code))return cache.get(code);const endpoint=String(window.DV_OPERATOR_BACKLOG_ENDPOINT||'').trim();if(!endpoint||!authHeader)return null;const r=await originalFetch(endpoint,{method:'POST',headers:{'content-type':'application/json',authorization:authHeader},body:JSON.stringify({action:'get',backlog_code:code})});const out=await r.json().catch(()=>null);if(r.ok&&out?.ok){cache.set(code,out);return out}return null}async function decorate(){const code=document.getElementById('selected-code')?.textContent?.trim();if(!code)return;const links=linksByFeedback.get(code)||[],container=document.getElementById('linked-backlog-list');if(!container||!links.length)return;for(const a of [...container.querySelectorAll('.backlog-link')]){if(a.querySelector('.feedback-relations'))continue;const backlogCode=a.querySelector('strong')?.textContent?.trim(),link=links.find(x=>x.backlog_code===backlogCode);if(!link)continue;let dependencies=link.dependencies,dependents=link.dependents;if(!dependencies&&!dependents){const out=await detail(backlogCode);dependencies=out?.dependencies||[];dependents=out?.dependents||[]}if(!a.isConnected||a.querySelector('.feedback-relations'))continue;a.insertAdjacentHTML('beforeend',relationLine('Depends on',dependencies)+relationLine('Depended on by',dependents))}}let queued=false;function schedule(){if(queued)return;queued=true;queueMicrotask(()=>{queued=false;decorate()})}window.addEventListener('DOMContentLoaded',()=>{const target=document.getElementById('linked-backlog-list');if(target)new MutationObserver(schedule).observe(target,{childList:true,subtree:true});schedule()})})();