# Operator routes

- `/operator/` — operator tools hub
- `/operator/backlog/` — canonical Backlog Manager
- `/operator/feedback/` — Feedback Inbox

Legacy `/feedback/operator/` and `/backlog/operator/` routes redirect to the canonical `/operator/...` taxonomy.
